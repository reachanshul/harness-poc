apiVersion: v1
kind: Service
metadata:
  name: {{ include "harness-poc.fullname" . }}
  labels:
    {{- include "harness-poc.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "harness-poc.selectorLabels" . | nindent 4 }}
